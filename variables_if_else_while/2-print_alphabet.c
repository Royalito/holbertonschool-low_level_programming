#include <stdio.h>
/**
 * main - Entry point
 * 
 * Description: 'the program's description'
 * @parameter: describe the parameter
 * 
 * Return: Always 0 (Success)
 */

int main(void)
{
int counter;
char newline = '\n';
counter = 'a';

while (counter <= 122)
{
putchar(counter);
counter++;
}
{
putchar(newline);
}
return 0;
}
