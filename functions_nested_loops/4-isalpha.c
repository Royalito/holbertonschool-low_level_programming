#include "main.h"
/**
 * main - Entry point
 *
 * Description: 'Uno si es minusca/mayuscula y cero si no lo es'
 *
 *
 * Return: Always 0 (Success)
 */

int _isalpha(int Bodoke)
{
if (Bodoke >= 'A' && Bodoke <= 'z')
{
return (1);
}
else
{
return (0);
}
}
