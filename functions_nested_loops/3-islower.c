#include "main.h"
/**
 * main - Entry point
 *
 * Description: 'imprime el alfabeto'
 *
 *
 * Return: Always 0 (Success)
 */

int _islower(int Bodoke)
{
if (Bodoke >= 'a' && Bodoke <= 'z')
{
return (1);
}
else
{
return (0);
}
}
