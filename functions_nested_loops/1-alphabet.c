#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int counter;
char newline = '\n';
counter = 'a';

while (counter <= 122)
{
_putchar(counter);
counter++;
}
{
_putchar(newline);
}
return (0);
}
